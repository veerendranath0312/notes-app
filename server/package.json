{
  "name": "notes-app",
  "version": "1.0.0",
  "description": "notes app backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../client && npm run build && cp -r dist ../server",
    "deploy:full": "yarn run build:ui",
    "lint": "eslint ."
  },
  "author": "veerendranath",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.6.2"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.4.1",
    "globals": "^17.6.0"
  }
}
